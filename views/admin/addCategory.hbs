<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
<link rel="stylesheet" href="/styles/addProduct.css">

    <!-- Navbar -->
    <div id="particles-js" class="snow"></div>
<script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
<nav class="navbar navbar-expand-lg navbar-dark bg-dark" style="background-color: #3D464D;">
  <div class="container-fluid">
    <div class="col-lg-4">
                <a href="" class="text-decoration-none">
                    <span style="color:#3D464D; background-color:#ffc800" class="h1 text-uppercase px-2">M</span>
                    <span style="color: #ffc800; background-color:#3D464D" class="h1 text-uppercase px-2 ml-n1">S</span>
                </a>
            </div>
    <a class="navbar-brand" href="/" style="color: #ffc800;">ADMIN PANEL</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav ms-auto">
        <li class="nav-item">
          <a class="nav-link active" aria-current="page" href="/admin/products" style="color: #ffc800;">Products</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="/admin/dashboard" style="color: #ffc800;">Users</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="/admin/category" style="color: #ffc800;">Category</a>
        </li>
        
        <li class="nav-item">
          <a class="nav-link" href="/admin/logout" style="color: #ffc800;">Logout</a>
        </li>
      </ul>
    </div>
  </div>
</nav>

<!-- Add Product Form -->
  <div class="container my-5">
  <h2 class="text-center add-product">ADD NEW CATEGORY</h2>
  <form action="/admin/addCategory" method="POST" class="product-form" enctype="multipart/form-data">
    <div class="form-group mb-4">
      <label for="name" class="text-white">Category Name:</label>
      <input type="text" id="name" name="category" class="form-control" placeholder="Enter product name" required>
    </div>

    <!-- New Input for Image -->
    <div class="form-group mb-4">
      <label for="image" class="text-white">Upload Image:</label>
      <input type="file" id="image" name="image" class="form-control" accept="image/*" required>
    </div>

    <!-- Image Preview -->
    <div class="form-group mb-4">
      <label class="text-white">Image Preview:</label>
      <div id="imagePreview" class="text-center">
        <img id="previewImg" src="" alt="Image Preview" style="max-width: 200px; display: none;">
      </div>
    </div>

    <div class="d-grid gap-2">
      <button type="submit" class="btn btn-warning">Add Category</button>
    </div>
  </form>
</div>
  <input id="message" value="{{message}}" type="hidden">
</div>
<script>
  const message = document.getElementById('message').value;

  // Show SweetAlert message if it exists
  if (message) {
    Swal.fire({
      icon: 'info',
      title: 'Note',
      text: message,
    });
  }

  const imageInput = document.getElementById('image');
  const previewImg = document.getElementById('previewImg');

  imageInput.addEventListener('change', function(event) {
    const file = event.target.files[0];
    if (file) {
      const reader = new FileReader();
      
      reader.onload = function(e) {
        previewImg.src = e.target.result;
        previewImg.style.display = 'block'; // Show the image
      };

      reader.readAsDataURL(file);
    } else {
      previewImg.style.display = 'none'; // Hide the image if no file is selected
    }
  });
</script>